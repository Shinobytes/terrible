export default class Appearance {
    constructor(gender, head, hairColor, body) {
        this.gender = gender;
        this.head = head;
        this.hairColor = hairColor;
        this.body = body;
    }
}
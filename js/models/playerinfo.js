export default class PlayerInfo {
    constructor(username, level, exp, x, y) {
        this.username = username;
        this.level = level;
        this.experience = exp;
        this.x = x;
        this.y = y;
    }
}